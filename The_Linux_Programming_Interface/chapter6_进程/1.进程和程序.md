## 6.1 进程和程序

- 进程：一个可执行程序的实例。
- 程序：包含一系列信息的文件。

- 执行一个可执行文件：
1. 启动终端，启动 shell。
2. 在 shell 进程下，为当前可执行文件调用 fork() ，向内核申请内存资源，创建子进程，并调用exec()，执行该程序。
3. shell 等待子进程结束。
4. 添加 & 参数，可异步进行两个进程。

- 进程的本质：由内核定义的抽象的实体，内含有内核为其分配的各项系统资源，供程序运行。

