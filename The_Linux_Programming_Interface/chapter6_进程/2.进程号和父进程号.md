## 6.2 进程号和父进程号

- 进程号：每个进程都有自己的唯一进程标识符(PID)
- 父进程号：每个子进程的父进程的唯一进程标识符

- 获取进程号和父进程号的方法：在 c 库内，在需要调用进程号，如作为一个函数的参数时，有getpid(), getppid()，可返回pid_t 类型的 PID。

- 进程的数量：在早期的linux版本中为32767，32位架构下，为32768，64位平台下，可达400万。
