## 6.4 虚拟内存管理

- 原因：由于每个电脑的内存大小有限，对于多进程同时运行的情况，会出现内存大小不足，无法同步运行，因此需要虚拟内存，为每个进程维护虚拟内存空间，并且有一一映射的页表, 即每个进程都有一个页表。而在内存中，由于内存有局部访问的特性，故对每个程序且分为一个个小的页单元，并且把部分页单元保存在物理内存中，需要访问的时候，便可把整个页加载到内存中，在之前，他们一直都在磁盘中的 swap 分区内。

- 这样做的优点：
1. 进程与进程，与内核之间隔离。
2. 多个进程可以共享。
3. 提高了 cpu 的利用率。
