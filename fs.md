# 磁盘分区（Disk Partition）详解

磁盘分区是将一块物理磁盘划分为多个逻辑区域的过程，每个分区在操作系统中被视为独立的逻辑磁盘。分区使系统能够更好地组织数据、安装多个系统、提高安全性并优化存储管理。

---

## 一、为什么需要磁盘分区？

### 1. **便于系统管理**

* 可将操作系统、用户数据、应用程序分开存储。
* 系统崩溃时，可只格式化系统分区，保护用户数据。

### 2. **支持多系统**

* 同一硬盘可安装 Windows、Linux 等多个操作系统，每个系统占用不同分区。

### 3. **提高安全性**

* 数据放在独立分区，可避免因为一个分区损坏而影响其他分区。

### 4. **提高性能**

* 文件系统可以按分区优化，减少碎片化，提升 I/O 性能。

### 5. **控制权限与加密**

* 不同分区可以设置不同的访问权限、加密方式（如 BitLocker、LUKS）。

---

## 二、磁盘分区的基础概念

### 1. **物理磁盘 vs 逻辑分区**

* **物理磁盘**：实际的硬盘设备，如 /dev/sda
* **分区**：物理磁盘的逻辑划分，如 /dev/sda1、/dev/sda2

### 2. **文件系统与分区区别**

* **分区**决定了磁盘区域的范围（逻辑划分）
* **文件系统**决定了数据的组织方式（如 ext4、NTFS）

---

## 三、两种分区表方案：MBR vs GPT

当前磁盘有两种主流分区方案：

---

## **1. MBR（Master Boot Record）分区表**

MBR 是传统的分区格式，诞生于 1980 年代。

### 1.1 MBR 的结构

MBR 位于磁盘第 0 个扇区（512 字节）：

* **引导加载程序（Bootloader）**：446 字节
* **分区表（Partition Table）**：64 字节
* **签名（55 AA）**：2 字节

### 1.2 MBR 的分区限制

* **最多 4 个主分区**
* 要更多分区需使用：

  * 3 个主分区 + 1 个扩展分区
  * 扩展分区内部包含多个逻辑分区

### 1.3 支持的磁盘容量

* 最大支持 **2TB**（因为 32 bit LBA addressing）

### 1.4 适用场景

* 旧电脑、老 BIOS 启动方式（Legacy BIOS）
* 小于 2TB 的磁盘

---

##  **2. GPT（GUID Partition Table）分区表**

GPT 是 UEFI 标准的一部分，是现代计算机的分区方式。

### 2.1 GPT 的优势

✔ 支持 **128 个分区**（取决于实现）
✔ 支持 **超过 2TB** 的磁盘
✔ 分区 GUID 标识，更安全稳定
✔ 元数据冗余存储，提高可靠性
✔ 与 UEFI 启动配合能力强

### 2.2 GPT 的结构

GPT 使用多个关键结构：

1. **保护性 MBR（Protective MBR）**
   防止旧工具误认为磁盘未分区。

2. **主 GPT 头（Primary GPT Header）**

3. **分区表（Partition Entries）**
   每个分区条目包含：

   * 分区类型 GUID
   * 分区起始/结束 LBA
   * 分区名称

4. **备份 GPT 头（Backup GPT Header）**
   用于防止 GPT 损坏。

### 2.3 适用场景

* 大于 2TB 的磁盘
* UEFI 启动方式
* 现代 Windows/Linux 系统推荐 GPT

---

## 四、磁盘分区类型（按用途）

### 1. **系统分区（System Partition）**

存放引导程序，如：

* EFI System Partition (ESP)
* Windows System Reserved

### 2. **根分区（Root Partition）**

Linux 中的 `/` 分区，用于存放系统核心文件。

### 3. **启动分区（Boot Partition / /boot）**

Linux 引导文件所在位置。

### 4. **交换分区（Swap Partition）**

用于虚拟内存（Linux）。

### 5. **数据分区（Data Partition）**

存储用户文件，比如：

* /home（Linux）
* D:（Windows 数据盘）

### 6. **隐藏分区**

用于系统恢复、出厂设置（OEM 分区）。

---

## 五、分区示例

### 1. **Linux GPT 分区示例**

| 分区             | 挂载点       | 大小    | 用途     |
| -------------- | --------- | ----- | ------ |
| /dev/nvme0n1p1 | /boot/efi | 512MB | EFI 引导 |
| /dev/nvme0n1p2 | /         | 30GB  | 根分区    |
| /dev/nvme0n1p3 | /home     | 100GB | 用户数据   |
| /dev/nvme0n1p4 | swap      | 8GB   | 交换分区   |

### 2. **Windows GPT 分区示例**

| 分区                       | 用途      | 说明     |
| ------------------------ | ------- | ------ |
| EFI System Partition     | UEFI 启动 | 必需     |
| Microsoft Reserved (MSR) | 保留区     | GPT 必有 |
| C:                       | 系统分区    |        |
| D:                       | 数据盘     |        |

---

## 六、Linux 下如何查看分区

### 1. lsblk

```
lsblk
```

### 2. fdisk（MBR）

```
sudo fdisk -l
```

### 3. parted（GPT）

```
sudo parted -l
```

---

## 七、磁盘分区的最佳实践

### 1. **系统与数据分离**

* 系统坏了重装不影响数据

### 2. **使用 GPT 代替 MBR**

除非旧电脑或兼容性原因，否则应该用 GPT

### 3. **适当规划分区大小**

* `/` 不宜过小
* `/home` 单独分区更安全
* Windows 用户尽量将 C: 与 D: 分离

### 4. **适当使用 Swap**

* 16GB 以上内存可以减少 Swap
* 需要休眠（hibernation）时 Swap ≥ RAM

---


# 文件系统详细介绍

## 什么是文件系统？

文件系统（File System, FS）是操作系统与存储介质（如硬盘、固态硬盘、闪存、磁带等）之间的接口。它的核心任务是负责文件的存储、管理、访问和保护。文件系统为操作系统提供了对存储设备的抽象，使得用户可以通过文件路径和操作命令（如打开、读取、写入）访问磁盘中的数据。

### 文件系统的核心功能

文件系统提供了多个核心功能，确保文件能够被正确、有效地管理：

1. **文件存储**：
   文件系统负责将数据存储在磁盘或其他存储设备上，并为每个文件分配磁盘空间。存储空间的管理方式直接影响文件存取的效率。

2. **文件的组织与结构**：
   文件系统提供一种组织文件的结构，通常以目录树的方式组织。每个文件存储在不同的文件块中，并由文件分配表或索引节点（inode）跟踪文件的位置。

3. **高效的数据访问**：
   文件系统通过维护文件索引、目录结构和数据块的管理，支持高效的文件存取操作。这些文件系统也实现了数据缓存（如页面缓存、目录缓存等）以提升性能。

4. **数据完整性与恢复**：
   现代文件系统提供日志功能，记录文件操作的变更，从而确保即使在系统崩溃后，也能通过日志恢复文件系统到一致状态。

5. **文件权限与安全**：
   文件系统提供访问控制机制，确保只有授权用户和进程才能访问和修改文件。不同的文件系统使用不同的权限模型来管理用户和进程的访问权限。

6. **磁盘空间管理**：
   文件系统负责管理存储设备上的空间，避免空间浪费，并提供文件碎片化管理、空间重用等策略。

## 文件系统的结构与工作原理

### 1. **超级块（Superblock）**

超级块是文件系统的重要组成部分，它包含了文件系统的元数据和基本结构信息，如文件系统的大小、空闲空间、文件系统类型等。操作系统在挂载文件系统时会读取超级块，来识别文件系统的结构和状态。

* **超级块的内容**：

  * 文件系统的大小、剩余空间。
  * 文件系统的类型。
  * 块大小、文件系统的总块数。
  * 数据块的分配情况。
  * 文件系统是否已经损坏、是否需要检查。

### 2. **文件分配表（FAT）/ 索引节点（inode）**

文件分配表（FAT）和索引节点（inode）是文件系统的核心结构，负责存储文件的元数据以及其在磁盘上的位置。

* **FAT（File Allocation Table）**：
  在 FAT 文件系统中，文件数据的存储位置由文件分配表（FAT）来记录。FAT 是一个连续的表格，每一项对应磁盘上的一个簇。文件的数据块由 FAT 表中的项来指示，整个文件的簇通过链式结构连接起来。

* **inode（Index Node）**：
  在类 Unix 系统中，inode 是文件的核心数据结构，每个文件都有一个 inode，包含了文件的元数据，如文件的权限、所有者、创建时间、修改时间、文件大小等信息。inode 还存储文件数据块的位置指针，允许操作系统高效地定位文件内容。

### 3. **数据块（Data Blocks）**

数据块是文件系统中实际存储数据的单元，文件由一个或多个数据块组成。每个数据块的大小通常为 4KB、8KB 或更大。文件内容被分配到数据块中，操作系统通过块号来管理和访问这些数据块。

* **数据块的管理**：
  文件系统必须有效地管理数据块，避免空间浪费或碎片化。文件系统使用数据结构如位图或链表来跟踪哪些块是空闲的，哪些块已被分配给文件使用。

### 4. **目录结构（Directory Structure）**

目录结构是文件系统中用于组织文件和目录的方式，通常采用树形结构。根目录是树的起点，所有文件和目录都是从根目录开始分层管理的。每个目录可以包含文件或子目录。

* **目录项（Directory Entries）**：
  目录项包含文件或目录的名称和其对应的 inode。目录结构不仅记录文件名和 inode 之间的映射关系，还能够支持路径查找和文件访问控制。

### 5. **空闲空间管理**

文件系统需要管理磁盘上的空闲空间，确保空间被高效地利用。常见的空闲空间管理方法包括：

* **位图（Bitmap）**：
  位图是一个二进制数组，每个位表示磁盘的一个块，0 表示该块空闲，1 表示该块已分配。

* **链表（Linked List）**：
  使用链表来跟踪空闲块，文件系统通过遍历链表来分配空闲块。

### 6. **日志（Journaling）**

现代文件系统（如 ext4 和 NTFS）通常使用日志记录技术（Journaling）来提高文件系统的可靠性。在文件系统发生更改（如写入数据、修改文件系统结构）时，操作首先记录在日志中。如果系统崩溃，文件系统可以根据日志恢复数据，避免数据丢失或文件系统损坏。

* **日志的作用**：

  * 提供快速恢复机制，确保数据一致性。
  * 记录文件系统的操作历史，防止文件系统崩溃导致不一致状态。

## 常见文件系统的详细比较

### 1. **FAT 文件系统**

* **版本**：FAT12、FAT16、FAT32。
* **特点**：

  * 最早由微软在 DOS 系统中使用。
  * FAT32 支持最大 2TB 的分区，最大 4GB 的单个文件。
  * 广泛支持于各类操作系统（Windows、Linux、macOS、嵌入式设备等）。
* **优缺点**：

  * **优点**：简单、兼容性好。
  * **缺点**：不支持文件权限，缺乏日志功能，文件管理效率低。

### 2. **NTFS 文件系统**

* **特点**：NTFS 是 Windows 操作系统的标准文件系统，支持高级特性如文件加密、压缩、磁盘配额、硬链接等。
* **优缺点**：

  * **优点**：支持大文件、文件权限管理、数据恢复、日志记录等。
  * **缺点**：性能相对较差，尤其在非 Windows 系统中兼容性差。

### 3. **ext 文件系统**

* **版本**：ext2、ext3、ext4。
* **特点**：ext 系列是 Linux 系统中的主要文件系统，ext4 是目前最常用的版本，支持大文件、大容量分区、日志记录等功能。
* **优缺点**：

  * **优点**：稳定、高效、支持大文件和大容量存储，ext4 性能较好。
  * **缺点**：不支持像 NTFS 那样复杂的权限管理，操作系统之外的兼容性差。

### 4. **APFS 文件系统**

* **特点**：APFS 是苹果为其所有设备（包括 macOS、iOS、iPadOS、watchOS、tvOS）设计的新一代文件系统，支持更高效的性能、更强的数据加密、空间共享等功能。
* **优缺点**：

  * **优点**：适合 SSD，支持高效加密和文件恢复，性能较优。
  * **缺点**：主要支持苹果设备，跨平台支持有限。

### 5. **ZFS 文件系统**

* **特点**：ZFS 是由 Sun Microsystems 开发的文件系统，具有高可扩展性和强大的数据保护功能，特别适合用于大规模存储系统。
* **优缺点**：

  * **优点**：数据完整性检查、快照、数据压缩等功能，适合用于企业级存储。
  * **缺点**：资源消耗较大，配置较复杂，支持平台有限。

## 文件系统中的性能优化技术

### 1. **缓存（Caching）**

文件系统通过缓存技术提升性能。操作系统会缓存最近使用的文件数据和目录信息，以避免频繁访问磁盘。常见的缓存包括：

* **页面缓存**：用于缓存文件的页面数据。
* **目录缓存**：用于缓存目录结构，减少磁盘访问次数。
* **磁盘缓存**：硬盘本身也有缓存机制，提升读写速度。

### 2. **延迟分配**

延迟分配是一种优化技术，文件系统会推迟文件数据块的分配，直到数据真正被写入磁盘时才进行分配，从而减少碎片化并提高性能。

### 3. **预读与预写（Read-Ahead & Write-Behind）**

操作系统通过预读和预写技术，提高磁盘 I/O 的效率：

* **预读**：在文件被访问之前，操作系统会预先读取文件的部分内容到缓存。
* **预写**：写操作不立即执行，而是先写入缓存，在适当的时候批量写入磁盘。

---
